// Adds an event listener for the 'click' event on the window object
window.addEventListener("click", () => {
  // Plays the audio element with the ID 'song' when the window is clicked
  document.getElementById("song").play();
});
